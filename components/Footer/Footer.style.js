import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container_footer: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "space-around",
    alignItems: "center"
  },
});